{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的权限：只用到内置的窗口/事件能力，加上用系统浏览器打开项目主页。文件读写走的是本应用自己的命令，不经过任何 fs 插件。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "comment": "open_url 会按 scope 校验目标地址，白名单为空时一律拒绝。这里只放行项目主页一个地址，别的什么都打不开。",
      "allow": [{ "url": "https://github.com/AIKnightPanda/claude-code-token-lens" }]
    }
  ]
}
